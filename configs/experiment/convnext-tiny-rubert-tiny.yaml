# @package _global_

defaults:
  - override /data: default
  - override /model: default
  - override /trainer: ddp

data:
  train_path: /home/sporkhun/CLIP/data/gcc_train.tsv
  val_path: /home/sporkhun/CLIP/data/gcc_valid.tsv
  test_path: /home/sporkhun/CLIP/data/gcc_valid.tsv
  max_length: 512
  batch_size: 600
  num_workers: 16

model:
  pos_weight: 100

trainer:
  min_epochs: 10
  gradient_clip_val: 5.0
  devices: 4

count_steps: 5_000
image_pretrained: facebook/convnext-tiny-224
text_pretrained: cointegrated/rubert-tiny2
embedding_dim: 192

seed: 0xFEED

tags: ["convext-tiny", "rubert-tiny", "gcc-val"]

task_name: "train-convext-rubert"
